CREATE TABLE users (
	id VARCHAR(50) NOT NULL,
	name VARCHAR(100),
	image_url VARCHAR(500),
	wins INT(11) UNSIGNED,
	losses INT(11) UNSIGNED,
	elo INT(11) UNSIGNED,
	PRIMARY KEY (id)
);

CREATE TABLE match_data (
	user1_id VARCHAR(50) DEFAULT NULL,
	user2_id VARCHAR(50) DEFAULT NULL,
	status VARCHAR(20) DEFAULT 'completed',
	winner_id VARCHAR(50) DEFAULT NULL,
	elo_diff INT(11) DEFAULT 0,
	updated TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

INSERT INTO match_data (elo_diff) VALUES(0);